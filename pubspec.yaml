name: cesium
description: A lightweight state management framework using ValueNotifier and Listenable with built-in Dio HTTP resources, pagination, targeted rebuilds.
version: 1.3.0
homepage: https://github.com/Youssef-Mahmoud-M/cesium
repository: https://github.com/Youssef-Mahmoud-M/cesium
topics:
  - reactive
  - ui
  - async
  - state-management

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  async: ^2.13.0
  dio: ^5.11.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
